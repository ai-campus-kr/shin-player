# 변경 기록

## 0.6.0-beta.7 — 2026-09-21

- 홈 화면의 큰 홍보 카드를 없애고, 하단 오른쪽에 높이 24px의 `KAI 교육 ↗`·`N 실전 AI ↗` 배지를 고정했습니다. 로컬·유튜브 영상 재생 중에도 표시됩니다.
- 검정 바탕과 흰색 글자에 선명한 보라색 `#603BFF`·네이버 초록색 `#03C75A` 로고를 사용합니다. 마우스를 올리거나 키보드로 선택하면 해당 브랜드 색 테두리가 나타납니다.
- 전체 이름은 툴팁으로 표시하고, 클릭하면 한국AI교육진흥원 홈페이지 또는 'ChatGPT도 모르는 AI실전활용법' 채널을 기본 브라우저에서 엽니다.

- 87개 통합 검사 통과. 네 가지 테마와 최소 창 크기, 재생 중 배지 표시 및 유튜브 채팅 배치를 확인했습니다.

## 0.6.0-beta.6 — 2026-09-21

- 탐색기·시작 메뉴·트레이·앱 제목 표시의 아이콘을 검정·흰색 S와 재생 삼각형으로 통일했습니다. 16~256px의 10가지 ICO 크기를 제공하고 아이콘 재생성 스크립트를 추가했습니다.
- 홈 화면 하단에 보라색 KAI 카드와 녹색 네이버 프리미엄콘텐츠 카드를 배치했습니다. 클릭하면 각각 한국AI교육진흥원 홈페이지와 'ChatGPT도 모르는 AI실전활용법' 채널을 기본 브라우저에서 엽니다.
- 영상 재생 시 홈 화면 카드가 함께 숨겨지며, 최소 창 크기와 재생목록을 연 상태에서도 주요 조작 버튼과 겹치지 않도록 간격을 조절했습니다.
- 미니멀 테마의 GIF 선택 영역에 짙은 청록색 채움과 밝은 민트색 손잡이·테두리를 적용해 선택 구간을 더 쉽게 구분하도록 했습니다. 키보드 포커스 표시도 같은 포인트 색을 사용합니다.
- 스튜디오·라이트·라임에서는 각 테마의 기존 포인트 색을 따르며, 열린 GIF 창에도 테마 변경이 반영됩니다.
- 87개 통합 검사를 통과했습니다. 경고를 오류로 처리하는 빌드와 실제 GIF 창의 네 가지 팔레트 렌더링을 확인했습니다. 구간 조절·변환 로직은 변경하지 않았습니다.

## 0.6.0-beta.5 — 2026-09-21

- 로컬·유튜브 GIF 창에 드래그 타임라인을 추가했습니다. 양 끝 손잡이로 시작·끝을 고르고 가운데 선택 영역을 끌면 길이를 유지하며 구간을 옮깁니다.
- 선택 확대·전체 보기, 방향키 0.1초 / Shift 1초 / Ctrl 10초 조정, Home·End 경계 이동을 지원합니다. 직접 입력과 현재 위치 버튼은 타임라인과 양방향 연동됩니다.
- 손잡이를 잡은 지점의 오프셋을 유지하고 마우스를 놓는 좌표까지 반영합니다. 손잡이 교차, 영상 범위 이탈, 드래그로 1시간을 초과하는 선택을 방지합니다.
- 시작·끝 입력을 나란히 배치하고 드래그 안내와 키보드 포커스 표시를 추가했습니다. 네 가지 테마와 최소 창 크기를 지원합니다.
- 87개 통합 검사 통과. GIF 드래그 좌표·창 크기·WPF 배율·경계·줌·키보드·입력 복구 검사를 추가했습니다. 10분→1분 실제 GIF 저장 검사도 드래그로 구간을 선택합니다.

## 0.6.0-beta.4 — 2026-09-21

- GIF 배속 직접 입력(0.25~100×)과 0.5/1/2/5/10/20× 빠른 선택을 추가했습니다. 시작·끝·배속을 바꾸면 예상 GIF 길이가 즉시 바뀝니다.
- 원본 구간 제한을 30초에서 1시간으로 늘리고 완성 GIF는 0.2초~5분으로 검증합니다. 예: 원본 10분을 10배속으로 1분 GIF에 저장.
- 로컬·유튜브 모두 원본의 전체 구간에 배속을 적용한 뒤 출력 FPS에 맞춰 프레임을 선택합니다. 저장 파일명과 완료 안내에 배속·결과 길이를 표시합니다.
- 유튜브는 화면 캡처 방식을 유지합니다. 배속은 저장 결과에 적용되며 캡처는 원본 구간 시간만큼 걸립니다. 빠른 GIF에 필요한 간격으로 화면을 저장해 불필요한 임시 프레임을 줄입니다.
- 긴 로컬 구간의 처리 대기 한도를 조정하고 취소·미완성 파일 정리·원래 재생 상태 복원을 유지합니다.
- 실제 10분 합성 영상을 10배속으로 60초 GIF로 저장하고 20초·40초의 장면 전환을 확인했습니다. 실제 유튜브 `wRgJPO_qIT4`에서도 10~20초 구간을 5배속의 2초 GIF로 저장하고 원래 위치를 복원했습니다. API는 호출하지 않았습니다.
- 84개 통합 검사 통과: 일반·소수·감속 GIF의 실제 길이와 장면 시점, 10분→1분 변환, 유튜브 배속을 한 번만 적용하는 동작, 입력 검증과 네 가지 테마를 포함합니다.
- 한국어·영어·일본어·중국어 README와 GIF 화면 캡처를 갱신했습니다.

## 0.6.0-beta.3 — 2026-09-21

- 메인 플레이어와 유튜브 창에 **GIF** 버튼 추가. 시작·끝 시간을 직접 입력하거나 현재 위치로 지정합니다.
- 0.2~30초 구간, 최대 크기 360/480/720px, 10/12/15/20fps를 선택해 무음 반복 GIF로 저장합니다. 자막과 API 키가 필요 없으며 GPT 토큰을 사용하지 않습니다.
- Windows 실제 사진 폴더의 `신플레이어 GIF`에 날짜·영상 이름·구간을 담아 저장합니다. 기존 파일은 보존하고 미완성 파일과 임시 프레임은 취소 시 정리합니다.
- 로컬 영상은 FFmpeg로 원본 구간을 변환하며 재생 위치를 유지합니다. 두 번 나누어 처리해 전체 디코딩 프레임을 메모리에 쌓지 않습니다.
- 유튜브는 WebView2에 렌더링된 영상 영역을 실시간 캡처합니다. 영상 스트림 URL을 읽거나 내려받지 않습니다. 화면 자막과 겹친 안내가 포함될 수 있고 실제 부드러움은 PC 성능에 따라 달라집니다.
- 유튜브 캡처 완료·취소 시 같은 영상의 위치, 배속, 음소거, 재생·일시정지 상태를 복원합니다. 광고·보호된 영상·실시간 방송을 거부하고, 영상 전환·최소화·화면 크기 변경·재생 중단 시 결과 저장을 멈춥니다.
- GIF 작업 중 창을 닫거나 앱을 종료하면 변환 프로세스 종료와 임시 파일 정리를 기다립니다.
- 네 가지 테마와 최소 크기에서 GIF 화면을 확인하고, 한국어·영어·일본어·중국어 README에 사용법과 실제 앱 캡처를 추가했습니다.
- GIF 시간 입력, 색상·장면 전환·해상도·반복, 중복 저장, 취소 및 창 닫기 정리, WebView2 영상 영역 캡처와 재생 상태 복원 등 **81개 통합 검사 통과**. GIF 검사는 합성 영상과 격리 브라우저를 사용하고 API를 호출하지 않습니다.
- 실제 사용자 제공 유튜브 영상 `wRgJPO_qIT4`의 10~13초 구간을 GIF로 저장하고 원래 일시정지 위치(약 5.815초)가 복원되는 것을 확인했습니다. 이 테스트에서도 API를 호출하지 않았습니다.
- Windows PowerShell 5에서 UTF-8 프로젝트 파일과 검사 결과를 읽을 때 생기던 인코딩 오류를 수정했습니다.

## 0.6.0-beta.2 — 2026-09-21

- 메인 화면의 링크 입력 팝업을 항상 표시되는 주소창으로 교체. 네 가지 UI, 최소 창 크기, 전체화면에서도 유지됩니다.
- Enter 또는 이동 버튼으로 열기, Alt+D / Ctrl+U로 주소 선택, Esc로 편집 취소. 주소 입력 중에는 문자·방향키·Space가 재생 단축키로 처리되지 않습니다.
- `https://` 없이 입력한 유튜브 주소, 홈·검색·재생목록 페이지를 지원하고 주소의 쿼리 매개변수를 보존합니다. 유튜브 창의 주소창도 동일하게 동작합니다.
- 유튜브 브라우저와 같은 창에 채팅 패널을 배치합니다. 넓은 창에서는 오른쪽, 좁은 창에서는 아래쪽이며 ‘채팅 배치’에서 직접 선택할 수 있습니다.
- 일반 질문에는 스크립트 기반 답변과 근거 시간 버튼을 표시하고 재생 위치를 유지합니다. 명시적인 이동 요청에만 이동합니다. ‘그 부분으로 이동해줘’는 직전 답변의 검증된 근거 시간을 재사용해 추가 API 호출 없이 처리합니다.
- 영상 변경 시 이전 자막·근거·질문 작업을 초기화하며 늦게 도착한 응답은 새 영상에 적용하지 않습니다. 자막을 읽은 뒤에는 유튜브 스크립트 패널을 접고 영상으로 돌아갑니다.
- 주소 입력·패널 배치·질문과 이동 구분·영상 전환을 포함한 74개 통합 검사. 명시적으로 실행하는 실제 API / 유튜브 검사 모드 추가. 일반 자동 검사는 실제 키를 읽거나 유료 API를 호출하지 않습니다.
- 실제 `gpt-5.4-mini`에서 일반 질문·이동 요청·이동 금지 요청을 검증했습니다. 실제 유튜브 영상의 한국어 스크립트 301개로 답변하고 재생 위치를 유지한 뒤, 후속 이동 요청으로 10:45에 도달했습니다. 광고 중 이동을 거부하는 동작도 확인했습니다.
- `.env`와 변형 파일을 Git에서 제외하고 공개 배포물에 포함하지 않습니다.
- 한국어·영어·일본어·중국어 사용 안내를 주소창 방식으로 갱신했습니다.

## 0.6.0-beta.1 — 2026-09-21

- **링크 / Ctrl+U**: 앱 내부의 Edge WebView2 브라우저에서 일반 유튜브 페이지를 엽니다. 영상 스트림 추출·다운로드 도구를 사용하지 않습니다.
- 유튜브에서 사용자가 **스크립트 표시**를 연 뒤 **자막 · AI 채팅 → 자막 불러오기**로 화면의 자막과 시간 정보를 읽습니다.
- **AI / Ctrl+J**: 로컬 영상의 선택한 내장 텍스트 자막을 읽어 질문합니다. 외부 SRT·이미지 자막·OCR·음성 받아쓰기는 분석하지 않습니다.
- **API 설정**에 본인 OpenAI 키를 입력하고 Windows DPAPI로 암호화해 저장·삭제합니다. 모델은 `gpt-5.4-mini`입니다.
- 질문과 자막 텍스트로 답변과 근거 구간을 찾고 첫 번째 구간으로 자동 이동합니다. 근거 시간 버튼으로 다시 이동할 수 있습니다. 영상·음성은 OpenAI에 전송하지 않습니다.
- 질문마다 입력/출력 토큰 수를 표시합니다. 긴 자막은 로컬 후보 검색과 시간대 표본을 합쳐 최대 60,000자 예산 안에서 선택하며, 부분 검색임을 알립니다. 전체 문맥 검색을 보장하지 않습니다.
- 잘못된 자막 ID, 근거 없는 답변, 취소, 닫힌 창과 영상 전환 후 응답은 재생 위치를 바꾸지 않습니다. 유튜브 광고 재생 중에는 이동하지 않습니다.
- 파일과 유튜브 링크 열기 작업을 직렬화하고 트레이로 숨긴 뒤 대기하던 링크가 창을 다시 띄우지 않도록 취소합니다.
- API 통신은 대체 응답으로 검증했으며 실제 유료 API 호출은 하지 않았습니다. WebView2의 DOM 자막 읽기·재생 위치 변경은 합성 페이지에서 검증했습니다. 실제 유튜브 스크립트는 확인 당시 빈 패널이 표시되어 끝까지 검증하지 못했습니다. 사이트 구조·로그인·영상 제한에 따라 사용할 수 없으므로 베타로 제공합니다.
- 기존 61개와 새 기능 10개를 합쳐 71개 통합 검사. 네 가지 UI와 기존 재생·캡처·증폭 회귀 검사 포함.
- 배포본의 첫 검사에서 기존 FFmpeg 자막 프레임 캡처가 한 차례 120초 시간 초과. 같은 바이너리의 전체 재검사는 71개를 통과했지만 일부 캡처 지연이 관찰됐으며 원인은 미확정입니다. 지연 문제가 수정됐다고 보장하지 않습니다.
- WebView2 Runtime은 유튜브 기능에만 필요합니다. 일반 Chrome/Edge/Whale의 프로필과 별도로 저장하며, 웹 재생에는 로컬 mpv의 배속·증폭·일괄 캡처 설정을 적용하지 않습니다.
- 배포 파일에 WebView2 SDK 라이선스/고지를 포함하고 한국어·영어·일본어·중국어 README를 함께 제공합니다.

## 0.5.0 — 2026-09-20

- 음량 슬라이더 옆 증폭 버튼과 메뉴 추가. 끄기·+3·+6·+9·+12 dB 선택.
- 로컬 오디오 증폭과 리미터 적용. 기본 음량·음소거와 별도로 조절하고 다음 실행에도 선택 유지.
- 증폭을 끄면 자체 필터만 제거. 다른 오디오 필터와 배속·음높이 보정·재생 상태 유지.
- 네 가지 UI에서 증폭 상태 표시 및 최소 창 배치 지원.
- 합성 PCM 음원의 실제 mpv 필터 출력에서 단계별 게인과 리미터 한도 측정. 설정 복원·연속 선택·음성 없는 영상·메뉴 검사 추가. 총 61개 통합 검사.

## 0.4.0 — 2026-09-20

- UI 선택 창 추가. 미니멀·스튜디오·라이트와 기존 라임 UI의 미리보기를 제공하고 즉시 적용.
- 스타일별 컨트롤 순서·밀도·타임코드 크기·재생목록 위치·색감 구분. 기본 스타일은 미니멀.
- 선택한 UI 저장 및 다음 실행 시 복원. 이전 설정 파일과 알 수 없는 스타일 값은 기본 UI로 복구.
- 재생 위치·배속·일시정지 상태를 유지하며 UI 전환. 기존 영상 출력 창을 재사용하고 열린 자막 캡처 창의 색상도 변경.
- 모든 스타일의 최소 창 배치, 전환 중 재생·탐색, 설정 직렬화, 선택 버튼과 대화상자 색상 검사 추가. 총 55개 통합 검사.
- 검사 중 연속 파일 전환 경로에서 네이티브 비정상 종료가 한 차례 발생. 이후 전체 재검사에서 재현되지 않았으며 원인은 미확정.

## 0.3.0 — 2026-09-20

- 차콜과 라임색 기반의 UI로 전면 개편. 파일 메뉴, 넓은 영상 영역, 재생·배속 컨트롤을 구분하고 상단에 자막 캡처 버튼 배치.
- 최소 창 크기와 재생목록 폭에 맞춰 시작 화면을 조정. 버튼의 호버·눌림·키보드 포커스, 자막 선택 창의 색상 통일.
- 재생 바가 같은 클릭 좌표를 두 번 반영해 다른 시간으로 이동하던 오류 수정. 실제 트랙과 손잡이 폭을 기준으로 절대 위치 계산.
- 드래그 중 마지막 이동 이벤트가 없어도 마우스를 놓은 위치로 탐색. 파일 변경·정지·트레이 전환 시 이전 탐색 동작 취소.
- 기존 45개 검사에 좌표 회귀 검사 6개 추가. 창 크기·재생목록별 28개 지점, 반복 이동, 캡처 해제, 전체화면과 WPF 레이아웃 배율 검사.
- UI 렌더와 버튼 겹침 검사, 자막 선택칸의 표시 이름과 항목 전환 검사 보강. 총 51개 통합 검사.

## 0.2.0 — 2026-09-20

- 내장 텍스트 자막별 일괄 캡처 추가. 각 구간의 중간 시점을 PNG로 저장.
- Windows 사진 폴더에 `날짜_영상이름_사진` 폴더 생성. 중복 폴더와 기존 사진 덮어쓰기 방지.
- 자막 언어/트랙 선택, 자막 포함 여부, 진행 수, 취소, 저장 폴더 열기 지원.
- 저장 사진의 자막·시간·완료/취소 상태를 캡처 목록에 기록.
- 재생과 독립된 로컬 처리. GPT/API 비용 없이 작동하며 도구가 없으면 검증한 FFmpeg를 최초 한 번 준비.
- 기존 재생 검사와 MP4/MKV 캡처·트랙 선택·취소·프레임 시간·한글 자막·WPF 창 검사를 합쳐 45개 통합 검사 통과.
- AI 검색·요약은 아직 미구현. 향후 모델은 `gpt-5.4-mini`, 내장 텍스트 자막이 있을 때만 지원하도록 범위 확정.

## 0.1.1 — 2026-09-20

- 신플레이어 자체 소스를 MIT 라이선스로 공개. 외부 엔진과 런타임의 기존 라이선스는 별도 적용.
- 재생 바를 클릭해도 이동하지 않던 WPF 이벤트 처리 오류 수정.
- 클릭·드래그·마우스 캡처 해제 후 탐색 처리, 한 번의 조작에서 중복 탐색 방지.
- 빠르게 파일을 연속해서 열거나 추가할 때 재생목록과 실제 재생 파일이 어긋나는 문제 수정.
- 파일 로딩 완료 후 외부 자막 추가, 이전 영상의 재생 완료 상태 초기화.
- 재생목록 방향키가 음량 조절로 처리되던 문제 수정.
- 현재 재생 항목을 지우면 다음 항목 재생, 마지막 항목을 지우면 정지.
- 재생 완료 후 Space를 누르면 처음부터 재생하도록 수정.
- 최근 기록 삭제 후 자동 저장으로 같은 영상이 다시 기록되는 문제 수정.
- 빠른 음소거 연속 조작, 중복 음량 명령, 초기 음량 표시 수정.
- 잘못된 설정값과 중복 기록 정리, 과도한 시간값으로 인한 예외 방지.
- 트레이로 닫을 때 처리 중이거나 대기 중인 파일 열기 취소.
- 파일을 여는 동안에도 단일 인스턴스 상태 조회와 종료 요청 처리.
- 업데이트 시 기존 자동 시작 설정 보존, 버전 정보 자동 반영.
- 공개 패키지에서 사전 빌드된 엔진 제외. 설치 시 고정된 원본 릴리스에서 내려받고 SHA256 확인.
- 기존 26개 검사에 회귀 검사를 추가해 37개 실제 WPF/libmpv 통합 검사 구성.

검사는 합성 로컬 미디어로 수행한다. 라우팅 이벤트 검사는 실제 WPF 이벤트 처리를 사용하지만 물리적인 마우스 입력을 자동화한 검사는 아니다. DRM, 장시간 4K/8K, HDR, 여러 모니터와 모든 DPI 조합은 별도 검증 대상이다.
